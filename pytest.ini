[pytest]
testpaths = tests
# Quiet output by default; CI uses -q anyway.
addopts = --tb=short
# Keep the suite focused on the public regression layer — anything
# author-side scratch lives under tests/_dev/ and is gitignored, but
# also collected-out here so a local copy doesn't drag the suite down.
norecursedirs = _dev __pycache__
