# Local-only reference copies of the legacy single-sensor toolboxes
# (landsat_toolbox.pyt, sentinel2_toolbox.pyt). Kept on disk for
# comparison but excluded from the repo — the active toolbox is the
# unified genesis_toolbox.pyt at root.
legacy/

# The public regression suite lives in tests/. Author-side scratch
# scripts (probes, A/B drivers, one-off audits) live in tests/_dev/
# and stay out of the repo — they reference local scene data and
# external GPUs that other users don't have.
tests/_dev/

# Local-only handoff doc: an open-source (arcpy-free) extract of the
# data-ingestion functions, generated for sharing with a colleague. Not
# part of the repo; the canonical code is genesis_toolbox.pyt at root.
docs/ingestao_dados_TAR_SAFE_HDF.md

# Python bytecode / cache
__pycache__/
*.py[cod]
*$py.class

# Pytest
.pytest_cache/

# ArcGIS Pro scratch artefacts
*.aux.xml
# Per-tool metadata sidecars (e.g., `landsat_toolbox.LandsatMosaic.pyt.xml`) —
# regenerated automatically by ArcGIS Pro and not portable across machines.
*.pyt.xml

# OS / editor crud
.DS_Store
Thumbs.db
*.swp
*~

# Editor / tooling local config — user-specific, not part of the repo
.vscode/
.idea/
